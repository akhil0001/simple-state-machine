{
  "name": "simple-state-machine",
  "version": "0.0.4",
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^8.0.0",
    "typescript": "^5.0.2",
    "vite": "^4.4.5",
    "vite-plugin-dts": "^3.6.3",
    "vitest": "^1.1.3"
  },
  "scripts": {
    "dev": "lerna run dev --parallel",
    "build": "lerna run build",
    "publish": "npm run build && lerna publish patch"
  }
}
